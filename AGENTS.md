# GeoViewer

Single-file HTML app using Mapbox GL JS. No build system, no dependencies.

## Usage

Open `geoviewer.html` directly in a browser.

## Data formats

- **CSV**: columns `name,lon,lat`
- **GeoJSON**: Point, LineString, Polygon, MultiPoint, MultiLineString, MultiPolygon, FeatureCollection

## Notes
